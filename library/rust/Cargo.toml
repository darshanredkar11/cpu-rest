[workspace]
resolver = "2"
members = [
    "cpurest-core",
    "cpurest-macros",
    "cpurest",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/cpurest/cpurest"

[profile.release]
lto = true
codegen-units = 1
panic = "unwind"
